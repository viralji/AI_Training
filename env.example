# =============================================================================
# AI TRAINING PLATFORM - ENVIRONMENT CONFIGURATION TEMPLATE
# =============================================================================
# Copy this file to .env and update the values
# IMPORTANT: Never commit .env file to git!
#
# Single source of truth for all environment variables
# Switch between DEV and PROD by commenting/uncommenting the appropriate sections
# =============================================================================

# =============================================================================
# DEVELOPMENT CONFIGURATION (for local testing)
# =============================================================================
# Uncomment DEV section and comment PROD section for local development
# =============================================================================

# === DEV: Server Configuration ===
NODE_ENV=development
PORT=3002
HOST=0.0.0.0

# === DEV: Frontend URLs ===
FRONTEND_URL=http://localhost:5173
CORS_ORIGIN=http://localhost:5173

# === DEV: Frontend Build Variables (VITE_ prefix) ===
# These are used by Vite during build time
VITE_API_URL=http://localhost:3002/api
VITE_BACKEND_URL=http://localhost:3002
VITE_SOCKET_URL=http://localhost:3002

# === DEV: Google OAuth ===
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3002/auth/google/callback

# === DEV: Security (use strong secrets in production) ===
JWT_SECRET=dev_secret_key_change_in_production
SESSION_SECRET=dev_session_secret_change_in_production

# === DEV: AI/Gemini ===
# Get from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key
GEMINI_MODEL=gemini-2.0-flash
GEMINI_MAX_TOKENS=1000
GEMINI_TEMPERATURE=0.7

# === DEV: User Roles ===
TRAINER_EMAILS=trainer@example.com
TRAINEE_DOMAIN=@gmail.com

# === DEV: Proxy / Cookies ===
TRUST_PROXY=0
SESSION_COOKIE_SECURE=false

# === DEV: Database Paths ===
DATABASE_PATH=./backend/database.sqlite
DATABASE_BACKUP_PATH=./backups
UPLOAD_PATH=./backend/uploads
SUBMISSIONS_PATH=./backend/uploads/submissions
MAX_FILE_SIZE=5242880

# === DEV: Email Configuration (optional for local testing) ===
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password_here
# EMAIL_FROM=your_email@gmail.com
# EMAIL_FROM_NAME=AI Training Team

# === DEV: Logging ===
LOG_LEVEL=info
LOG_FILE=false
LOG_FILE_PATH=./logs/app.log
HEALTH_CHECK_ENABLED=true

# =============================================================================
# PRODUCTION CONFIGURATION (for Digital Ocean server)
# =============================================================================
# Uncomment PROD section and comment DEV section for production deployment
# =============================================================================

# === PROD: Server Configuration ===
# NODE_ENV=production
# PORT=3002
# HOST=0.0.0.0

# === PROD: Frontend URLs ===
# FRONTEND_URL=https://aitraining.cloudextel.com
# CORS_ORIGIN=https://aitraining.cloudextel.com

# === PROD: Frontend Build Variables (VITE_ prefix) ===
# VITE_API_URL=https://aitraining.cloudextel.com/api
# VITE_BACKEND_URL=https://aitraining.cloudextel.com
# VITE_SOCKET_URL=https://aitraining.cloudextel.com

# === PROD: Google OAuth ===
# GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=your-google-client-secret
# GOOGLE_REDIRECT_URI=https://aitraining.cloudextel.com/auth/google/callback

# === PROD: Security (REQUIRED: Change these to strong secrets!) ===
# JWT_SECRET=your-super-secure-jwt-secret-key-minimum-32-characters-long
# SESSION_SECRET=your-super-secure-session-secret-key-minimum-32-characters-long

# === PROD: AI/Gemini ===
# GEMINI_API_KEY=your-gemini-api-key
# GEMINI_MODEL=gemini-2.0-flash
# GEMINI_MAX_TOKENS=1000
# GEMINI_TEMPERATURE=0.7

# === PROD: User Roles ===
# TRAINER_EMAILS=trainer@example.com
# TRAINEE_DOMAIN=@gmail.com

# === PROD: Proxy / Cookies ===
# TRUST_PROXY=1
# SESSION_COOKIE_SECURE=true

# === PROD: Database Paths ===
# DATABASE_PATH=/var/www/AI_Training/backend/database.sqlite
# DATABASE_BACKUP_PATH=/var/www/AI_Training/backups
# UPLOAD_PATH=/var/www/AI_Training/backend/uploads
# SUBMISSIONS_PATH=/var/www/AI_Training/backend/uploads/submissions
# MAX_FILE_SIZE=5242880

# === PROD: Email Configuration ===
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password_here
# EMAIL_FROM=your_email@gmail.com
# EMAIL_FROM_NAME=AI Training Team

# === PROD: Logging ===
# LOG_LEVEL=info
# LOG_FILE=true
# LOG_FILE_PATH=/var/www/AI_Training/logs/app.log
# HEALTH_CHECK_ENABLED=true

# =============================================================================
# HOW TO SWITCH BETWEEN DEV AND PROD:
# =============================================================================
# 1. For LOCAL DEVELOPMENT: Keep DEV section uncommented, PROD section commented
# 2. For PRODUCTION: Comment DEV section (add # to each line), uncomment PROD section (remove #)
# 3. Update PROD values with your production secrets and URLs
# 4. Save the file and restart the application
#
# Quick switch example:
#   - Comment DEV: sed -i 's/^NODE_ENV=/#NODE_ENV=/g' .env
#   - Uncomment PROD: sed -i 's/^#NODE_ENV=/NODE_ENV=/g' .env
# =============================================================================

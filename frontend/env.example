# Frontend Environment Variables

# Backend API URL (full URL with /api)
VITE_API_URL=https://aitraining.cloudextel.com/api

# Backend Base URL (without /api) - optional, will be derived from VITE_API_URL if not set
VITE_BACKEND_URL=https://aitraining.cloudextel.com

# Socket.io URL - optional, will use backend URL if not set
VITE_SOCKET_URL=https://aitraining.cloudextel.com

